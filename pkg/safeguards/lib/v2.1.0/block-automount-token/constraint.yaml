apiVersion: constraints.gatekeeper.sh/v1beta1
kind: K8sAzureV2BlockAutomountToken
metadata:
  name: v2-azure-block-automount
spec:
  match:
    kinds:
      - apiGroups: [""]
        kinds: ["Pod"]
  parameters:
    excludedImages: {{ .Values.excludedImages }}
