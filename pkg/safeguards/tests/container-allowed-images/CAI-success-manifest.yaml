apiVersion: apps/v1
kind: Deployment
metadata:
  name: cai-success-manifest
spec:
  replicas: 3
  selector:
    matchLabels:
      app: my-app
  template:
    spec:
      containers:
        - name: my-container
          image: nginx:latest