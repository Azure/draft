apiVersion: apps/v1
kind: Deployment
metadata:
  name: test
  labels:
    kubernetes.azure.com/generator: draft
spec:
  template:
    spec:
      containers:
        - name: test
          image: acr.test:latest